/* ============================================
   RESULTS DATA — sample-results.js
   Royal Crystal Academy
   ============================================
   Phase 4: Real scores are stored in PostgreSQL
   and loaded via the API (api.js).

   This file now provides an empty object.
   localstorage.js will restore any saved scores.
*/

(function () {
  if (!window.SAMPLE_RESULTS) {
    window.SAMPLE_RESULTS = {};
  }
})();
